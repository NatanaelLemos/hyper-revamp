# Rust / Cargo build output
/target
**/*.rs.bk
*.pdb

# bundled app output
/dist

# local dev config (repo root only — assets/schema.json is a real source file)
/hyper.json
/schema.json

# macOS
.DS_Store

# editors
.idea
.vscode/*
!.vscode/launch.json
